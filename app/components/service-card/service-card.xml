<StackLayout class="card">
    <Label text="{{ name }}" class="text-lg font-semibold" />
    <Label text="{{ description }}" class="text-gray-600" />
    <Label text="{{ 'SAR ' + pricePerHour + '/hour' }}" class="text-blue-500" />
</StackLayout>