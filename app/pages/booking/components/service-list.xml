<StackLayout>
    <Label text="Select Service" class="text-lg font-bold mb-2" />
    <ListView items="{{ services }}" class="mb-4">
        <ListView.itemTemplate>
            <component:service-card />
        </ListView.itemTemplate>
    </ListView>
</StackLayout>