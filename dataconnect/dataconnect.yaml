specVersion: "v1beta"
serviceId: "Relaxly"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Relaxly-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
